<!DOCTYPE html>
<html lang="en">
{{> baseHead}}
<body>
    {{> siteHeader }}
    <div>
        <h2>สวัสดี ยินดีต้อนรับ</h2>
        <p>วันนี้เพื่อนมีอะไรจะฝากถาม ....</p>
        {{#each allPosts }}
            <div style="background-color: rgb(254, 203, 203);">
                <h4><a href="/p/{{ this.id }}">{{ this.title }}</a></h4>
                <p>
                    <span>{{ this.from }} | </span>
                    <span>{{ this.createdAt }}</span>
                </p>
                {{#if this.commentsCount }}
                    <p>{{this.commentsCount}} คอมเม้นท์</p>
                {{else}}
                    <p>ยังไม่มีคอมเม้นท์</p>
                {{/if}}
            </div>
        {{/each}}
    </div>
    {{> siteFooter }}
</body>
</html>